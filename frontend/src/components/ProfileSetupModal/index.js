import React from 'react';
import './ProfileSetupModal.css';

const ProfileSetupModal = () => {
  // Authentication has been removed, so this component is no longer needed
  return null;
};

export default ProfileSetupModal;